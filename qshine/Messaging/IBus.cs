using System;

namespace qshine
{
	/// <summary>
	/// Indicate a message bus
	/// </summary>
	public interface IBus
	{
		//void Send<T>(T message) where T : IMessage;
		//void Register<T>(T handler) where T : IMessageHandler;
	}
}
